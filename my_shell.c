#include "shell.h"
#include <stdio.h>
#include <stdlib.h>

/**
  * main - simple shell
  * Return: 0 if succesful
  */
int main(void)
{
	/*
	if (argc > 2)
		return (-1); */
	/* check if argv[1] is a file */

	/* signal handler */

	/* read, tokenize, execute loop */
	main_loop();

	/* clean up */

	return (0);
}
